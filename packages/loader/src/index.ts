/**
 * Adapter: globs i18next JSON locale files and flattens nested keys to `KeyId`s.
 *
 * Stage 0 scaffold: intentionally empty of loading logic.
 */
export const PACKAGE_NAME = '@i18n-xray/loader'
