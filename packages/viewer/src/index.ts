/**
 * Renders `report.json` into a single self-contained HTML report — inline
 * CSS/JS, zero network requests.
 *
 * Stage 0 scaffold: intentionally empty of rendering logic.
 */
export const PACKAGE_NAME = '@i18n-xray/viewer'
