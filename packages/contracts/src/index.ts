/**
 * Zod schemas shared across the toolchain: `report.json`, `FileScan`, and the
 * module-graph port. `report.json` is the only interface between the analyzer
 * and the viewer.
 *
 * Stage 0 scaffold: intentionally empty of schemas.
 */
export const PACKAGE_NAME = '@i18n-xray/contracts'
